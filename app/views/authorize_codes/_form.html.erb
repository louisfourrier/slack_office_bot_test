<%= form_for(@authorize_code) do |f| %>
  <% if @authorize_code.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@authorize_code.errors.count, "error") %> prohibited this authorize_code from being saved:</h2>

      <ul>
      <% @authorize_code.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br>
    <%= f.text_area :code %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
